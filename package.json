{
  "name": "solana-spl-airdrop",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@types/node": "^17.0.31",
    "ts-node": "^10.7.0"
  },
  "scripts": {
    "start": "ts-node ./src/index.ts",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
